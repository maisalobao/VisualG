Algoritmo "somadinho"

Var
// Seção de Declarações das variáveis 
   num, contador, soma: inteiro

Inicio
// Seção de Comandos, procedimento, funções, operadores, etc...
   contador <- 0
   Enquanto contador < 4 faca
      escreva("Quer somar quanto, amore?")
      leia(num)
      contador <- contador + 1
      soma <- soma + num
      escreval(soma)
   FimEnquanto
Fimalgoritmo